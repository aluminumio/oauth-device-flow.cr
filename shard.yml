name: oauth-device-flow
version: 0.1.0
crystal: ">= 1.10.0"

authors:
  - Aluminum.io <hello@aluminum.io>

description: |
  OAuth 2.0 Device Authorization Grant (RFC 8628) client for Crystal CLIs.
  Full UX, auto-refresh, pluggable token store, zero runtime dependencies.

license: MIT
